from distutils.core import setup
setup(
  name = 'nodemanager',
  packages = [''], 
  version = '0.1',
  description = "Seattle's nodemanager package",
  author = 'Seattle',
  url = 'https://github.com/BuildSeash/nodemanager', # use the URL to the github repo
  classifiers = [],
)